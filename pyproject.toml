[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "compas-scan"
version = "2.0.0"
description = "AI-First competitor intelligence tool"
requires-python = ">=3.11,<3.12"

[tool.uv]
dev-dependencies = []